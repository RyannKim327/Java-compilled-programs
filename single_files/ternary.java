public class ternary{
	public static void main(String[] args){
		int num1 = 5;
		int num2 = 10;
		System.out.printf("%d is %s than %d", num1, ((num1 > num2) ? "greater" : "less"), num2);
		System.out.println((true) ? "True" : "False");
		// Format (boolean) ? If (true) : else (false)
	}
}